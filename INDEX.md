# INDEX

> ⚠️ **2026-09-18:** see **`HANDOFF.md`** for the build under active development
> (`Swordforge_looptest_landscape.html`). The "WHERE CANON LIVES NOW" table below predates it.

Chronological catalog of every doc in this repo. `canon` = current source of truth;
`superseded`/`deprecated` marked when it happens.

## WHERE CANON LIVES NOW

| Topic | Canonical doc |
|-------|---------------|
| What this repo is | [README.md](README.md) |
| Repo rules / conventions | [CLAUDE.md](CLAUDE.md) |
| Game design / mechanics SSOT | [specs/game-design.md](specs/game-design.md) |
| **Which build is canon** | **[Swordforge_looptest_landscape.html](Swordforge_looptest_landscape.html)** — the only build under development, and since 2026-09-22 what the Pages root serves. Historical: [swordforgeV2.html](swordforgeV2.html) (V2), [v1.html](v1.html) (V1, was `index.html`), [Swordforge_new_looptest.html](Swordforge_new_looptest.html) (portrait, deliberately never touched). The older per-section split below is superseded for this build. |
| Zero-context handoff / onboarding | [ONBOARDING.md](ONBOARDING.md) |
| Task tracker (done / next up) | [plan.md](plan.md) |
| Lessons learned | [LEARNINGS.md](LEARNINGS.md) |
| Session-to-session state | [HANDOFF.md](HANDOFF.md) |
| Code navigation (wiki, subordinate to specs) | [docs/wiki/index.md](docs/wiki/index.md) |
| Agent workflow config (issues/labels/domain) | [docs/agents/](docs/agents/) |

## Timeline

- **(pre-2026-08-10)** — [archive/Sword_Grid_Game_GDD.md](archive/Sword_Grid_Game_GDD.md) — original design vision. `superseded` — superseded-by `specs/game-design.md`.
- **(pre-2026-08-10)** — [specs/game-design.md](specs/game-design.md) — mechanics SSOT, kept in sync with the code. `canon` — supersedes the archive GDD. **Updated 2026-08-12 (16746d9):** now a two-build split — §2 steps 2–4 + §3–§5 are canon per the loop-test (Path-Forge craft loop); §6–§8 still reflect index.html. Integration open (§9).
- **(pre-2026-08-10)** — [ONBOARDING.md](ONBOARDING.md) — zero-context contributor/agent handoff (describes V1 systems; see its canon-update banner). `canon`
- **(pre-2026-08-10)** — [plan.md](plan.md) — running done/next-up tracker. `canon`
- **(pre-2026-08-10)** — [research/chalk-map-design.md](research/chalk-map-design.md) + [research/map_test.html](research/map_test.html) — chalk-map redesign exploration (not shipped). `canon` (research)
- **(pre-2026-08-10)** — [research/potioncraft.md](research/potioncraft.md) — PotionCraft reference notes. `canon` (research)
- **(pre-2026-08-10)** — [research/sword-forge-gdd.html](research/sword-forge-gdd.html) — Sword Forge GDD (research doc; now **v0.3**, Path-Forge redesign — see 2026-08-11 below). `canon` (research, forward-looking; specs/game-design.md is the built-state SSOT)
- **2026-08-10** — agent workflow config from setup-matt-pocock-skills: [docs/agents/issue-tracker.md](docs/agents/issue-tracker.md) (GitHub Issues via `gh`), [docs/agents/triage-labels.md](docs/agents/triage-labels.md) (five-role vocabulary), [docs/agents/domain.md](docs/agents/domain.md) (single-context CONTEXT.md + ADRs). `canon`
- **2026-08-10** — [AGENTS.md](AGENTS.md) — agent-file pointer to the code wiki (written by wiki adopt). `canon`
- **2026-08-10** — [docs/wiki/](docs/wiki/) — code-repo navigation wiki adopted (subordinate to the spec chain). `canon` (navigation layer)
- **2026-08-10** — V2 (`swordforgeV2.html`) declared the canonical build; `index.html` (V1) kept for historical reference and remains the deployed Pages entry point until promoted. Recorded in CLAUDE.md / README / ONBOARDING banners. **Superseded 2026-09-22 (below).**
- **2026-09-22** — **the Pages root now serves the build under development.** `index.html` had been V1 since the beginning, so opening the deployed site silently gave a build that had been superseded twice, and the landscape loop-test was reachable only by typing its filename. V1 moved to `v1.html` and `index.html` became a 20-line redirect to `Swordforge_looptest_landscape.html`. **A redirect, not a copy** — the game is one self-contained file, and duplicating it at two paths would mean two files to keep in step, which is how the canonical-build confusion started. `.claude/hooks/spec-sync-warn.js` now guards `v1.html` instead of `index.html` (verified firing). Closes the "promoting V2 to the root is a pending decision" note open in CLAUDE.md since 2026-08-10.
- **2026-08-10** — living-docs spine seeded by `start-session` (INDEX.md, LEARNINGS.md, HANDOFF.md, verifier hook, `.claude/settings.json`).
- **2026-08-11** — [research/sword-forge-gdd.html](research/sword-forge-gdd.html) bumped to **v0.2 → v0.3**: the **Path-Forge** core-loop redesign (PotionCraft-style ore-path navigation). `canon` (research, forward-looking design).
- **2026-08-11** — [story/bram-one-more-sunrise.html](story/bram-one-more-sunrise.html) — Bram story vignette. `canon` (narrative).
- **2026-08-12** — [Swordforge_new_looptest.html](Swordforge_new_looptest.html) — the **Path-Forge loop-test build** (standalone 9:16; ore→grind→smelt→hammer-travel→water-quench→shape). `canon` (build — craft loop only; not wired to economy/Vault, see specs §9).
- **2026-08-12** — **specs/game-design.md reconciled** (16746d9): the loop-test is now canon for the ore→sword craft/movement loop (§2 steps 2–4, §3–§5); economy/UI (§6–§8) stay index.html. *Done as an in-place edit — no dated decision doc (cf. the stranded `specs/2026-08-10-core-loop-potioncraft-mapping.md`, which lives only in the `core-loop-mapping` worktree, never landed).*
- **2026-08-13** — [research/Shop Titans Crafting Tycoon/](research/Shop%20Titans%20Crafting%20Tycoon/) — Shop Titans competitive teardown corpus (game-research skill). `canon` (research, block-indexed reference library).
- **2026-08-13** — [Potion Craft/](Potion%20Craft/) — Potion Craft FTUE teardown (video-analysis; text deliverables only, media gitignored — see `Potion Craft/MANIFEST.md`) + [research/swordforge-ftue-flow.md](research/swordforge-ftue-flow.md) — tri-color Path-Forge FTUE flow keyed to specs §refs, FigJam Mermaid. `canon` (research + design).
- **2026-08-13** — [specs/2026-08-13-looptest-phase1-vert-anchor-match.md](specs/2026-08-13-looptest-phase1-vert-anchor-match.md) — Phase 1 portrait match to the vert anchor: 2-col grid (HUD bar + vertical right ore rail zone) + diorama recomposed via LAYOUT + cozy CSS (structure pass), then AFK NB-Pro green-screen art gen (chroma-keyed, ≤$10). Records the Phase 1 portrait LAYOUT composition. `canon` (build spec, branch `sword-forge/dual-orientation-anchor-rework`).
- **2026-08-13** — [specs/2026-08-13-looptest-layout-skeleton.md](specs/2026-08-13-looptest-layout-skeleton.md) — Phase 0 layout-skeleton refactor of `Swordforge_new_looptest.html`: CSS-Grid named zones (hud/map/bench) + two `grid-template-areas` (portrait/landscape) + data-driven `LAYOUT` table (fractions of zone, both orientations) + `resolveLayout`/`applyLayout` + `?wire` wireframe + `?test` self-test (in-zone/parity/drift invariants). Records the portrait ground-truth positions. `canon` (build spec, branch `sword-forge/dual-orientation-anchor-rework`).
- **2026-08-14** — [Swordforge_looptest_landscape.html](Swordforge_looptest_landscape.html) — the **landscape (hori-anchor) loop-test build**: a separate 1080×600 file (copy of the portrait loop-test) flipped to the hori composition — left wood-plaque HUD column + Skill Tree top-right, full-width parchment map (anchor map cut → `assets/map/territory_hori.png`), 2-col wood ore rail, and the bench diorama laid out horizontally (dragon · anvil+hammer · furnace+cauldron · grinding wheel · bucket · mug). New anchor cuts: `assets/forge/anchor_grindwheel.png`, `assets/forge/anchor_cauldron.png`. `canon` (build — landscape look; portrait `Swordforge_new_looptest.html` untouched).
- **2026-08-14** — [specs/2026-08-14-looptest-phase2-landscape-hori-anchor.md](specs/2026-08-14-looptest-phase2-landscape-hori-anchor.md) — Phase 2 landscape match to the hori anchor: records the landscape `LAYOUT` composition (RECORDED, **loop r12**), the separate-file decision, and the two loop eras — (1) Opus-gated composition r2 58→r7 77, then (2) the **objective-gate era** (r8–r12) using `tooling/anchor-match/` (`screen_gate` palette + `element_size`), which drove the map over-saturation fix, anchor re-cuts of the **smelter** (seated pot, Opus 38→71→80), **grey-stone HUD plaque** 9-slice, **winged dragon**, and **bellows** (79 SHIP), plus owner-directed hero-sizing (furnace/anvil 1.5×, dragon/mug 2×), the map/bench seam fix, and `?clean` render mode. `canon` (build spec, branch `sword-forge/dual-orientation-anchor-rework`).
- **2026-08-14** — [specs/2026-08-14-looptest-station-minigame-feel.md](specs/2026-08-14-looptest-station-minigame-feel.md) — Phase B station-feel pass on `Swordforge_looptest_landscape.html`: wired the previously-dead grind wheel (`wireGrindWheel` + `#grindHot` hold-to-grind), added a shared `fxBurst` particle emitter, and per-station juice (grind sparks, bellows embers + heat-ramped furnace glow, anvil strike sparks + recoil, quench steam + flash, dragon fire embers). `canon` (feel spec, branch `sword-forge/looptest-minigame-feel`).
- **2026-08-14** — [tooling/anchor-match/README.md](tooling/anchor-match/README.md) — objective anchor-matching tooling + notes: what transfers from `mother-clucker`'s camera gate and `art-pipelines-RND/consistency-gate` (deterministic measure→gate + per-project spec + mandatory chroma-key), and two Sword-Forge gates — `prop_gate.py` (single-cut fidelity: silhouette IoU + luminance + colour MAE, magenta key) and `screen_gate.py` (region-localized composition diff: per-region palette histogram distance + coarse-luminance structure, with a review heatmap). PIL-only, TDD (14 tests). Baseline: landscape palette-match **75.1/100**, worst regions rail 0.55 / skilltree 0.35 / map 0.32. `canon` (tooling, branch `sword-forge/anchor-match-tooling`).
- **2026-08-18** — [assets/Anchor-images/landscape-scale-ref.png] + [tooling/anchor-match/spec.landscape-scale-ref.json](tooling/anchor-match/spec.landscape-scale-ref.json) — owner's greyscale **scale reference** for the landscape build, and the measured element boxes taken from it. Drove loop **r13** on `Swordforge_looptest_landscape.html`: map row 54%→75.5% and every prop re-scaled to the reference element-% (smelter 1.44×, anvil 2.09×, grinder 1.87×, dragon 2.07× too big → all 1.00×), superseding the r11–r12 hero sizing. Also fixed `.dragoncrop`'s hardcoded 255px (LAYOUT never sized the dragon) and added the hammer to `FRAME_BOX_SEL`. Followed by owner-directed sizing **r14** (grinder 2x, anvil 1.5x, dragon 1.5x, mug 2x, hammer 0.5x) and **r15** (grinder -30%, anvil -20%, hammer +75%). Recorded in [specs/2026-08-14-looptest-phase2-landscape-hori-anchor.md](specs/2026-08-14-looptest-phase2-landscape-hori-anchor.md). `canon` (build spec, branch `sword-forge/anchor-match-tooling`).
- **2026-08-18** — [specs/2026-08-18-looptest-landscape-map-traits-hazards.md](specs/2026-08-18-looptest-landscape-map-traits-hazards.md) — **landscape map rework**: painted `territory_hori.png` removed for a flat beige board (so trait/hazard discs are visible and truthful again), 5 starter traits (Sharp/Durable/Flexible/Heavy/Balanced) placed inside a tightened opening reveal with the other 19 scattered far out under the fog, `INIT_REVEAL` 1500→430 + `INIT_W` 2800→1100, and **hazards made real** (blade integrity 100, 26 dmg/sec while travelling, shatter = run + traits lost). Then **r17**: trait catalog redrawn from the owner's map sketch — 24→**25 traits** (+Swift/Cloud/Rainbow/Magma/Mist/Poison, Flame→Fire, Storm→Gale, −Sharp/Accurate/Honor/Endurance/Cruel), positions driven by a `TRAIT_POS` table measured in the sketch's own pixel space rather than ore-path ends. Then **r18/r19**: the four downward ore paths (ember/frost/tide/gale) scaled x0.5 twice, to x0.25 of original — reach 661-686 -> 165-172, which is now short of even the nearest trait (heavy @197), inverting them from the longest routes to the shortest. Then **r20**: right-column UI rebuilt from the owner reference (`Reference/landscape test.png`) — rail 13%->21%, 4 map-selection tabs on the map edge, a day/gold/popularity card + SKILL TREE, and a MATERIALS inventory (3x4, all 8 ores, decorative RAIL_FILLER dropped); left HUD column cleared; LAYOUT x/w rebased x1.1013 so the widened rail did not shrink the owner-approved prop sizes. Then **r21**: blade-trait panel top-left — 5 trait slots (a tier-N trait fills N slots with its element icon; Weak/Fine/Epic = 1/2/3), a cancel-crafting button wired to `resetRun()`, and a placeholder save button; the old `#sfTraits` strip over the map removed. Then **r22**: dragon/hammer/mug drag bounds moved from the bench rect to the whole frame (`dragBox()`), so they reach the top of the screen again — the bench is only ~24.5% tall since the map grew, which had capped them at ~0.70 of frame height. Then **r23**: the released ingot appeared on the bellows — `snapToGate()` used furnace-fraction 0.72 (the smelter's far right, where the bellows sits) instead of the gate at 0.27; the orb position now derives from `#furnaceGate`'s live rect (at gate-fraction 0.62/0.70, inside the painted arch opening). `#furnaceGate` itself is unchanged — an intermediate attempt to move it was reverted, and the harness faults that caused it (viewport mismatch between screenshot and dump-dom runs; an in-flow probe div squashing the frame) are recorded in the spec. Portrait deliberately untouched — the two loop-tests now diverge in catalog as well as layout. `canon` (build spec, branch `sword-forge/anchor-match-tooling`).
- **2026-09-18** — [specs/2026-09-15-looptest-landscape-tutorial-script.md](specs/2026-09-15-looptest-landscape-tutorial-script.md) — **tutorial script SSOT** for `Swordforge_looptest_landscape.html`: the copy and ordering for **D1–D139** (permanent ids, retired not reused), the dragon's `#dragonSay` bubble spec, the counter-side `#screenDragonSay`, and per-line trigger/pointer/wording notes. Records every wording correction made against the owner's drafts (door→gate, orb→hot metal/metal ingot, craft room→forge, gleaming→beaming, grinder→grinding wheel, skill points→exp books, and for D44–D47 a missing tag-question comma, lowercase fire→**FIRE**, and click→**tap** everywhere because this is a mobile game) and the rule that the dragon guides until the script's **last** line has played, then becomes a pet. **Extended r154–r196 (2026-09-23, PRs #16/#17):** new lines **D69a/b/c** (the first hazard crossing stops the world, once per game), **D94b**, **D130a** (Garric's solo run, with a retry that restores the ore in full — deliberately NOT `refundOre()`, which is the Restore Ore talent and returns nothing unranked), **D67a** (the five parts of the map); **D30 deleted**; D4, D42, D128 reworded. Records the rule that a new id is **inserted beside its neighbour, never appended**, because `lastLine()` is the map's final key and drives the guide-to-pet switch (still `D139`, now 144 keys). Also the pointer vocabulary completed: the "tap to continue" hint became a **button** with a double-play / X icon that lights on read-on lines (`SAY_GO_GLOW`), `SAY_GO_MORE` for ids whose run ends but whose beat does not, `SAY_AFTER` as the one table for what follows a line once it leaves the screen, and `sayChoose(list, optional)` for an offer that can be walked past. `canon` (script SSOT — the build follows it, not the reverse; branch `sword-forge/tutorial-script-and-craft-systems`, then `tutorial-polish`).
- **2026-09-18** — [specs/2026-08-18-looptest-landscape-map-traits-hazards.md](specs/2026-08-18-looptest-landscape-map-traits-hazards.md) **extended r60→r128** (same dated doc, appended per its established per-round pattern): recipe book + update-composition, the cave & pickaxe ore-gathering, shop racks + locks + passive sale, the exp/level/talent system and four blue skills, then the **tutorial build-out** — dragon dialogue system, smelter gate gated on *heat* not a timer (r83), the Sword Crafted window (r90), the hammering minigame's fire aimed by holding the **metal** with an analytic solver (r87), a quench mug + cancel (r88), per-trait Design Desk asset sets (r91), the counter **bell** + customers who ask for a trait 80/20 discovered/undiscovered (r96), and a grinding wheel driven by **angle swept** rather than hold-time (r99); then **r102** shapes and quenches the second sword (D44–D47), whose two idle prompts re-fire every 4 s of inactivity and whose mug pointer had to be lifted over the z-1000 minigame panel; then **r103** takes that sword to the basement, sharpens it (D48–D55, a third dialogue surface on the dragon icon, the edge capped at the green band) and sells it, makes **sharpening and a custom design worth +7g each** on a widened sell button that prints the terms, and fixes two locks it walked into (no counter sale was possible after Bram, and his intro panel returned on every visit). Then **r104** carries the script past that sale (D56–D65): the customer quotes the price and the dragon the bonus through **{g}/{b} tokens filled at speak time**, the bell brings an adventurer who wants a **fire sword**, and **Fire moves to (836,1070)** — the measured end of 2 fully ground iron + 2 manganese at ~75%, with the manganese grind capped at 0.80 because a full grind ends inside a hazard. Then **r105** gives the counter dialogue boxes a single owner: the scripted customer panel sits over `.cs-dlg` and nothing emptied it, so a departed customer's line stayed on screen behind the next one (a fault dating to r97, not r104). Then **r106** clears five faults from a play session: the scripted customer's line was `pointer-events:none` so the conversation could not be advanced at all; `sayNext()` restarted at **D1** whenever a run ended and `SAY_I` drifted from what was on screen; `tryAcquire()` stacked the SAME trait on every quench (Balanced x8, 352g); `.cs-resp` was a third unowned box; and the mug pointer skipped D10. **r107** then restores D1-D4, which r106 had killed: `SAY_I = -1` meant both "not started" and "finished", and the opening rode on the restart branch r106 removed. **r108** keeps the second customer at the counter for his own farewell: `sellCounter()` clears `CUSTOMER`, which is what draws a generic portrait, so he vanished the instant he paid and left his reaction and the Thank you button on an empty counter. **r109** keeps the fire-sword request (D62/D63) on screen: hiding the customer panel as the dragon answered let the counter's own never-written box show in its place, blanking the order. **r110** makes the second craft insist on a **full grind**: raw drops on the furnace and part-ground ore off the wheel are both refused during the `regrind-*` stages, which is the run that teaches grinding, while the first craft (raw by design) and the fire craft (manganese at ~75%) are untouched. **r111** forges the fire sword (D66–D78) and **corrects r104**: the recipe is 2 iron + 1 manganese ground full, then a SECOND manganese capped at 75%, so Fire moved again to (838,1024) and the cap now decides the tier (Fine, every time); r104s hazard justification was measured before its own trait move and does not hold, because moving a trait rearranges the hazard field. **r112** stops the FIRST craft hijacking later ones: `tutGateStep()` was a catch-all that fell through to D5 for any craft it did not special-case, dragging D6-D9 behind it and overwriting the stage r111 waited on. **r113** moves Fire 14 units LEFT of the route X it was sitting on top of, and replaces the spot-glow: a #tutSpot ring drawn above the fog, with Fire own disc hidden until the sword reaches it, because the X reveal alone already opens the fog over it. **r114** then places Fire per the owners sketch, BELOW the full-grind X (53 down, 3 left), caps the last manganese at **70%**, and unhides the disc so the fog cuts it in half at the spotting moment (centre fogged, a third of the rim clear). **r115** gives the DRAGON answer buttons of his own (D79–D81): a #sayChoices cluster hung under whichever bubble he is speaking from, which blocks tap-to-advance while an answer is pending. **r116** decorates the fire sword (D49 re-used, D82–D85): the design desk gains the dragon-icon surface, now a shared `.tut-iconsay` class, and DONE marks the sword designed so it carries its +7g. **r117** re-uses **D54** after D84 so the decorated sword goes back in the bag before the counter, and adds `sayQueue()` because `sayNext()` walks `SAY_SEQ` in insertion order and a re-used line sits too far back to be reached by a tap. **r118** sells the fire sword and ends Day 1 (D23 re-used, D86–D88): the price in her thank-you is interpolated (74g, not the drafted 54g), the sleeping dragon on the bed speaks D88, and a bubble clamp that had never been tested on the right-hand side was letting the box slide under the inventory rail. **r119** then removes the second dragon from the bedroom: Brams beat writes an INLINE display on #screenDragon, which outlives any class rule, so that inline style now has one owner. **r120** replaces the screen-change ARROW with a blinking glow on the exit button itself (all 14 guides converted, `tutExit()` owning the class off the arrow clearing path), and opens **Day 2** in the bedroom so there is a walk down to the forge to guide. **r121** extends the blink to the record step (the blade-panel toggle and RECORD CRAFT), generalising it to a halo that works on any control while the pan buttons keep their amber fill. **r122** opens **Day 2** (D89–D94): the skill tree must actually be SPENT in before the run moves on, the bell brings a scripted man2 asking for **gale**, and the cave holds its full five seams (35 swings to clear, flagged). **r123** re-cuts **copper** (`len` 225 → 144) so two fully ground ones stop **30 units past Swift**: Weak on arrival, Epic only after pulling back with the dragon fire (D95, D96). **r124** then TEACHES that pull for the first time (D97–D99) and makes it **park itself exactly on the trait** while it is being taught, so holding the fire too long cannot overshoot. **r125** adds the **gale run** (D100–D104): three ground copper, a fourth ground, then one **unground** (the first stage to invert the grind rule, wheel capped at 0), with Gale ringed at the "almost there" beat. **r126/r127** finish and sell it (D105–D109) and bring **Bram back** for a swift sword (D110–D114, D44 re-used): the fire-pull clamp becomes target-driven so it parks on Gale as well as Swift, and BramD2 reuses the scripted-customer panel. **r128** makes an ingot a real **drag** from the bag onto the anvil (ghost, anvil highlight, drop test) instead of a tap, so D113 describes what the game does. **r129** makes Bram gift a **diary** (D115–D119): a Claim window on `assets/forge/diary.png`, the diary re-paged to **1 Travel Log / 2 People I met**, and the DIARY and QUEST rail buttons **hidden until this beat** (saved as `rail:{diary,quest}`, with a pre-r129 save read as already unlocked); r129b rewinds an early diary close back to the button instead of stranding the blink inside a hidden modal. **r130** brings **Garric** (D120–D130): the bell lights with D119, man4 arrives as `???` and lectures down one of two branches that rejoin at his name reveal, the customer panel learns to queue a LIST of lines rather than one, June leaves the diary and Garric takes her slot (gated on `DIARY_MET`), and after the dragon's reply the script hands the craft over entirely; **r131** grades the sword he gets (D131–D139): three outcomes on the Balanced trait's tier plus designed/sharpened, a flat +10g on the top two, and a `DD_BONUS`/`PARTS_GARRIC` gate that holds new Balanced design parts until the best outcome pays out (the lists ship empty, pending art); **r132** makes outcome 1 pay **nothing**, decided inside `sellCounter` because the sold hooks run after the gold, the ledger row and the toast are already done; **r133** fixes outcome 3 being unreachable: a trait is `{t,…}` on the bench but `{tid,…}` on a finished sword, and r131 read only the former, so every real sword graded its Balanced trait as Weak (r131’s fixture was hand-built in the bench shape and agreed with the bug); r130b gives the `g-` stages their own branch of the screen dispatcher (the r127 gap again) and re-glows the bell after a screen change. Each round records its measured verification and the bugs that verification caught. `r138` takes the loop-test scaffolding off the bench at the owner's request (the numbered step badges, the station captions, the Bellows tag and the dragon's tap-to-talk, plus the bucket caption and the counter dragon's twin, which are the same two families): hidden rather than deleted, since JS writes the dragon tag and LAYOUT places the captions, with `?labels` to bring the set back. `r139` adds the heat gauge (`#heatMini`) to that set with the MECHANIC untouched - `updateGauges()` still drives `#furnaceGlow` and the gate still opens at `HEAT_READY`, so the furnace mouth brightening is the feedback - and corrects r138's restore rule, which used `display: revert` and would have brought the flex-centred badges and gauge back mis-drawn. `r140` puts each ore's NAME above its icon in the rail and stops drawing ores you do not have (the greyed `.out` placeholders were five of seven slots on day 1); `shelfOres()` had to stop returning `ORES.length` since `buildShelf()` pads the grid from that return. **Extended r141–r196 (2026-09-23, PRs #16/#17):** ~40 rounds of play-test polish — the dragon gains a **position per screen**; nothing can be dragged under the inventory panel (one `railEdgeX()` serves the counter dragon, the cave pickaxe, and `dragBox()`'s bench drags, the last of which needed the **painted** overhang measured rather than `offsetWidth`, because the mug is rotated about an off-centre origin); a day transition that fades the whole frame to black; the Sword Crafted window at 1.5x; map controls moved off the grindstone to the top right; a SKIP HAMMER cheat parented **outside** `#frame`; the route flashing outward as each of the first craft's four ores lands; and a gale keep-out that removes one seeded hazard **after** placement rather than rejecting it during, so the rest of the board does not reshuffle. Two rules worth carrying: **a halo near a clipped edge does not read** (`#rail`/`#frame` are `overflow: hidden`, so a control within ~20 px of an edge needs a face swap, not a box-shadow), and **`TUT_TICK_STAGES` gates the idle pointer** — a new `tutNudgeApply()` branch is dead unless its stage is listed. `canon` (build spec; branch `sword-forge/tutorial-script-and-craft-systems`, then `tutorial-polish`).
- **2026-09-21** — [specs/2026-09-21-asset-payload-diet.md](specs/2026-09-21-asset-payload-diet.md) — **asset payload diet** for `Swordforge_looptest_landscape.html`: the build downloaded **50.57 MB across 43 images before first paint** (348 MB decoded if resident, above the iOS Safari tab ceiling) because almost every file was stored at many times the size it is drawn at — `iron_ore` is 1552 px wide for a **70 px** icon, `pickaxe` 1984 px for **54 px**. Records the rule (**2x the widest measured on-screen box, stored as WebP**, art untouched), the six reasoned overrides (`map_base` capped at 2048 because it is a pannable SVG world rather than a box; the five scene backgrounds and `hammer_bg` at 1920; `plaque_stone` left at natural width because its `border-image` slice is in SOURCE pixels), and how it was encoded **with no image tooling on the machine** — the browser encodes and a dev-only `/__save` in `.claude/serve.js` writes the bytes. Result **1.39 MB first paint / 2.30 MB total / 95.5 MB decoded**, quality MAE 0.5-3.1 per channel at display size. **Extended 2026-09-23:** the same browser pipeline converted five more — `weak`/`fine`/`epic` (314 KB → 20 KB at 248 px), `Bram_illustration` (989 KB → 154 KB, re-encoded at native 844 px rather than downscaled, so it is shown at ~1.5x not the 2x the rule asks — the source has no more pixels) and `MAP_parts` (1626 KB → 85 KB at 800 px). Manifest 140 → 145 entries, so a re-run includes them. Also records the trap that cost the most: nine sites **build** their asset path at runtime (`ddSrc`, the blade / midblade / portrait / ore families), so a manifest built from a render sweep missed **64 files**, and a grep for `.png` would have reported the rewrite complete. `canon` (art sizing + encoding; branch `tutorial-phase2`).
- **2026-09-21** — [specs/2026-09-21-mobile-viewport-fit.md](specs/2026-09-21-mobile-viewport-fit.md) — **mobile viewport fit** for `Swordforge_looptest_landscape.html`: the owner's 1356x610 Chrome Android phone cut ~215 px off the bottom, because `#frame` is a fixed 1080x600 box and a viewport meta sizes the WIDTH only. Records the chosen formula `max(1080, ceil(600 * innerW / innerH))` written into the viewport meta at head-parse, on `resize` and twice after `orientationchange`, so the browser zooms the page out until 600 CSS px fills the visible height and the slack becomes side bars; plus `#frame { flex-shrink: 0 }` for the second defect the measurements found (any viewport under 1080 px squeezed the 1.8:1 aspect and moved every `LAYOUT` prop with it), and `safe` flex centring so a sub-1080 desktop window can still scroll to the frame's left edge. Rejects a `transform: scale()` wrapper (120 sites read `getBoundingClientRect`/`clientX`, and `tutRect()` mixes scaled and unscaled coordinates) and a fluid re-layout (519 lines of hardcoded px). Verified at the owner's aspect (meta `width=1334`, whole frame visible, no scroll, 127 px bars each side), at 360x800 portrait (unchanged) and at 1024x768 desktop (aspect held). Guarded by `tooling/mobile-fit/fit.test.mjs`, which extracts `sfFitWidth` out of the HTML so a drifting edit goes RED. Also measures the separate payload problem: **80.8 MB over 74 assets, 348 MB decoded if resident**. `canon` (build spec, branch `sword-forge/mobile-compat`). **r135** adds the portrait **rotate gate** (`sfWantsRotate`: a coarse pointer plus `innerH > innerW`, an overlay that asks rather than `orientation.lock`, which only works inside fullscreen on Android and would spin the game under an upright player), moves `touch-action: none` onto `#frame` wholesale (with `#oreShelf` keeping `pan-y`) instead of surface by surface, and clears the `?test` RED that predated the branch: r93 moved the dragon to -3.683 on purpose and updated neither `RECORDED` nor the landscape in-zone floor, so both followed it (the dragon carries his own floor of -3.70 so the other seven keep the tighter -2.80).
