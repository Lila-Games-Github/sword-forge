# Sword Forge code wiki - first bounded seed pass

Date: 2026-08-08 (session date). Repo HEAD at time of seeding: `0f30699d0126de40830dcedd7acb84653cb8d52f` (`0f30699`).

## Status
DONE

## Pages created (8 total, cap 10 not hit)

Concepts (`docs/wiki/concepts/`):
- `Grid Exploration.md` - the 50x50 fog-of-war grid, movement chokepoint, Purify dash mini-game, hazards, death.
- `Trait Heating.md` - the heating mini-game (pulley/bellows/stabilize) and the first two stages of the quality-tier chain.
- `Forging Pipeline.md` - the shape/hammer/quench/design/sharpen pipeline (`forgeSword`/`quickCraft`), pause/resume, last two quality-chain stages.
- `Customer Economy.md` - active NPC selling loop, scripted/story customers (Bram/June/Roland), payout formula.
- `Day System.md` - the 7-customers-per-day pacing wrapper and End Day transition.

Entities (`docs/wiki/entities/`):
- `Recorded Compositions.md` - saved trait+metal blueprints (`savedBlueprints`) and the Auto-Craft entry point.
- `Passive Shop.md` - the 500g-unlock passive sell channel, sell-tick loop, ledger.
- `Tutorial Flow.md` - the data-driven `tutorialFlow` onboarding state machine (the cross-cutting integration point).

Plus `docs/wiki/index.md` updated (Concepts/Entities sections populated) and `docs/wiki/log.md` appended with a `## [2026-08-08] ingest | codebase seed (first pass)` entry.

## Anchor verification
Every page's anchors use `kind: file` with a literal `probe` string. Wrote a small verification pass (grep-equivalent substring check) against every anchor's `path` after finishing all 8 pages, then re-ran it after a global em-dash cleanup (see Concerns below) to make sure no edit broke a probe. Result both times: **0 failures** across all 25 anchors (mix of `index.html` and `swordforgeV2.html` targets). Every probe grep-hits its file.

## Deliberately NOT covered (deferred to a later pass)
- **Smelter / raw-metal economy** (`startMetalGenerator`, lowest-first priority fill, smelter upgrades) - closely related to Grid Exploration but not routed as its own page.
- **Quests** (`quests`, `bumpQuest`) and the **Diary** (`diaryCustomers`/`diaryGiven`) - UI/progress layers on top of Customer Economy and Day System.
- **Value/tier formulas** (`specs/game-design.md` §5 "Sword value & tiers") and **quality overlays** (crack/sparkle) - downstream of Forging Pipeline, not separately anchored.
- **Minimap/Chart, camera pan/zoom, dev-cheat box** - UI wrapping, not load-bearing systems.
- The **chalk-map prototype** (`research/map_test.html`) - explicitly out of scope per task instructions (research/, not shipped).
- `swordforgeV2.html` was only used to add a few parallel-build cross-anchors (`move`, `forgeSword`, `tutorialFlow`, `CUSTOMERS_PER_DAY`) on relevant pages; it was not separately mapped page-by-page.

## Concerns
- My first draft of all 8 pages used em-dashes (`\u2014`) in prose, violating the vault-wide em-dash ban (a global user policy, not specific to this repo, but still binding). Caught it with a grep sweep before finishing, ran a script to replace ` \u2014 ` with ` - ` across all 8 files, then re-verified every anchor still grep-hits post-edit (it does). No em-dashes remain in `docs/wiki/`.
- No commits were made. `git status --short docs/wiki` shows `?? docs/wiki/` (untracked, staged-for-owner) - the owner's pre-existing uncommitted WIP elsewhere in the repo was left untouched; nothing outside `docs/wiki/` was read-write besides the canon-reading pass (index.html, swordforgeV2.html, specs/, CLAUDE.md, ONBOARDING.md, plan.md - all read-only).
- `specs/game-design.md` is thorough and mostly matches code (spot-checked every anchor); I did not attempt a full line-by-line spec/code reconciliation beyond confirming the anchors I cited actually exist and match the described behavior at a skim level.

## Report path
`C:\_BISU\_WORKSPACE\AI_Explorations\_Claude\sword-forge\docs\wiki\.seed-report.md`
